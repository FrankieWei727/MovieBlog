{
  "scripts": {
    "postinstall": "cd front && npm install",
    "postbuild": "cd front && npm run build",
    "start": "cd front && npm start"
  },
  "engines": {
    "node": "13.0.1",
    "npm": "6.12.0"
  }
}